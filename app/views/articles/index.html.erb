<h3><%= @category.name %></h3>

<ul>
  <% @articles.each do |article| %>
  <li><%= link_to article.title, category_article_path(article) %></li>
  <% end %>
</ul>

<%= link_to 'All Categories', categories_path %>
<%= link_to 'Create Article', new_category_article_path %>

